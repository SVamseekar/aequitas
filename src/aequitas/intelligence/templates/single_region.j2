{% if region_name and value is not none -%}
{% set hib = higher_is_better|default(true) -%}
{% set is_favourable = (vs_national_pct >= 0) == hib -%}
**{{ region_name }}** records **{{ value|round(2) }} {{ unit }}**, {{ vs_national_pct|round(1)|abs }}% {{ "above" if vs_national_pct >= 0 else "below" }} the national average of {{ national_avg|round(2) }} {{ unit }}.

{% if is_favourable -%}
This places {{ region_name }} ahead of the national benchmark, suggesting the region's existing service model — operator coverage, route density, or LTA investment — is performing relatively well on this measure. Maintaining this position should be a policy priority as funding decisions are made under the Bus Services Act 2025.
{%- else -%}
This places {{ region_name }} behind the national benchmark, indicating a gap that LTA investment, BSIP funding, or franchising powers under the Bus Services Act 2025 could help close. Closing even part of this gap would be a concrete policy implication for regional transport planning.
{%- endif %}

{% if is_lad_level_unfiltered -%}
*Note: this is a Local Authority District (LAD)-level metric and cannot be subdivided by urban/rural area type — LADs typically contain a mix of urban and rural LSOAs. The figures shown are for the full {{ region_name }} region regardless of the urban/rural filter.*
{%- endif %}
{%- endif %}
